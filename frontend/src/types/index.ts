export interface IContact {
  name: string;
  lastName: string;
  phoneNumber: string;
  email: string;
  age: number;
  avatar: string;
  linkToWebsite: string;
  tags: string; // comma-separated string
}
